# base

[agent]
  hostname = "seed"
  omit_hostname = false
  interval = "5s"

[[outputs.influxdb]]
  urls = ["http://influxdb:8086"] # required
  database = "telegraf" # required

[[inputs.cpu]]
  percpu = true
  totalcpu = true
